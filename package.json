{
  "private": "true",
  "name": "headless-cms",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap --no-sort && lerna link --no-sort",
    "build": "lerna run build --stream --no-sort",
    "dev": "lerna run dev --stream --no-sort"
  },
  "devDependencies": {
    "lerna": "^5.2.0"
  }
}
