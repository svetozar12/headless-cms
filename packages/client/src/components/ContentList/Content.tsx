import { useState } from "react";

const ContentList: React.FC = () => {
  // const [isModal, setIsModal] = useState(false);

  return (
    <div className="flex h-screen flex-col items-center bg-mainBlack"></div>
  );
};

export default ContentList;
